{
  "title": "TODO: short title",
  "description": "A containerised service has exited and will not auto-restart. container.state shows restart_policy=no and status=exited. The task simulates the ops procedure: update the restart policy to unless-stopped and mark the container as running to restore service.",
  "lesson": "",
  "hint_1": "check the restart policy in container.state",
  "hint_2": "change to unless-stopped",
  "hint_3": "also set status=running to complete the state transition",
  "hint_4": "sed -i 's/^restart_policy=.*/restart_policy=unless-stopped/;s/^status=.*/status=running/' /opt/winlab/docker-container-crash/container.state",
  "success": "Incident resolved.",
  "fail": "Still failing. Check the logs."
}
