{
  "title": "TODO: short title",
  "description": "seed.sh overwrites /etc/ssh/sshd_config with two faults and leaves sshd stopped:.",
  "lesson": "After any sshd_config change, always run sshd -t before restarting.",
  "hint_1": "check config: /usr/sbin/sshd -t && cat /etc/ssh/sshd_config",
  "hint_2": "fix port: sed -i 's/^Port 2222/Port 22/' /etc/ssh/sshd_config",
  "hint_3": "Fix password auth: sed -i 's/^PasswordAuthentication no/PasswordAuthentication yes/' /etc/ssh/sshd_config",
  "hint_4": "Start sshd: /usr/sbin/sshd — confirm: ss -tlnp | grep ':22 '",
  "success": "Incident resolved.",
  "fail": "Still failing. Check the logs."
}
